Validator module: setup and recommendations

Files:
- `setup.sh`: sample installer and directory layout
- `validator.service`: systemd template for running `solana-validator`
- `hardware.md`: recommended hardware and networking
- `key_management.md`: secure key handling guide

Notes:
- This module contains placeholders for mainnet configuration. Replace endpoints, key paths, and flags before production use.
- NEVER commit identity keypairs to source control.
